/*
求1~n之间所有的素数并输出（在函数使用上有三个阶段：
1.直接在main函数中完成这一系列过程（可用性低且难懂）
2.只使用一个函数来实现（更高懂但是可用性仍不高）如果问题变得复杂那么这个函数就无法使用了
如：求1000以内每个数到一之间的素数
3.把要做的事情模块化 不要把一个函数弄得过于复杂 要要独立 单一（这样敲出来的代码质量才高 即：模块思想）
*/
#include <stdio.h>

bool IFprime(int num)
{
	int x;
	for (x = 2; x < num; x++)
	{


	}

}

int main()
{



	return 0;
}