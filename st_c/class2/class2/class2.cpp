#include <stdio.h>

int main()
{
/*
1.
	double r;
	double pai;
	double S;
	double V;
	scanf_s("%lf", &r);
	pai = 3.14;
	V = pai * r * r;
	S = pai * r * 2;

	printf("%lf\n%lf", S, V);
*/



/*
* 2.
	int c;
	scanf_s("%d", &c);

	if (c <= 100 && c >= 90)
		printf("A");
	else if (c < 90 && c >= 80)
		printf("B");
	else if (c < 80 && c >= 70)
		printf("C");
	else if (c < 70 && c >= 60)
		printf("C");
	else
		printf("D");
*/


/*
* 2.
	double a, b, c;
	scanf_s("%lf %lf %lf" ,&a, &b, &c);
	if (a >= b && a >= c)
		printf("%lf", a);
	else if (b >= a && b >= c)
		printf("%lf", b);
	else if(c >= b && c >= a)
		printf("%lf", c);
*/



/*
 4.
	double x, y;
	scanf_s("%lf", &x);
	if (x < 1)
	{
		y = x;
		printf("%lf", y);
	}
	else if (x >= 1 && x < 10)
	{
		y = 2 * x - 1;
		printf("%lf", y);
	}
	else if (x >= 10)
	{
		y = 3 * x - 11;
		printf("%lf", y);
	}
*/

	return 0;
}